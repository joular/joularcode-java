<?xml version="1.0" encoding="UTF-8"?>

<!--
  ~ Copyright (c) 2026, Adel Noureddine.
  ~ All rights reserved. This program and the accompanying materials
  ~ are made available under the terms of the
  ~ GNU Lesser General Public License v3.0 (LGPL-3.0-only)
  ~ which accompanies this distribution, and is available at
  ~ https://www.gnu.org/licenses/lgpl-3.0.en.html
  ~
  ~ Author : Adel Noureddine
  -->

<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>org.noureddine.joular</groupId>
    <artifactId>joularcodejava</artifactId>
    <version>0.0.1</version>
    <packaging>jar</packaging>
    
    <name>${project.artifactId}</name>
    <description>Joular Code Java is a lightweight and efficient Java agent for monitoring the energy consumption of methods and executions branches</description>
    <inceptionYear>2026</inceptionYear>
    <url>https://github.com/joular/joularcode-java</url>
    
    <licenses>
        <license>
            <name>The GNU Lesser General Public License v3.0 (LGPL-3.0-only)</name>
            <url>https://www.gnu.org/licenses/lgpl-3.0.en.html</url>
        </license>
    </licenses>

    <developers>
        <developer>
            <name>Adel Noureddine</name>
            <email>adel.noureddine@outlook.com</email>
        </developer>
    </developers>

    <scm>
        <connection>scm:git:git://github.com/joular/joularcode-java.git</connection>
        <developerConnection>scm:git:ssh://github.com:joular/joularcode-java.git</developerConnection>
        <url>https://github.com/joular/joularcode-java</url>
    </scm>

    <properties>
        <maven.compiler.release>21</maven.compiler.release>
        <encoding>UTF-8</encoding>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <mockito.version>5.23.0</mockito.version>
        <!-- Default so surefire's @{argLine} still resolves when no agent (JaCoCo and the like) has set it -->
        <argLine></argLine>
    </properties>

    <dependencies>
        <dependency>
            <groupId>org.junit.jupiter</groupId>
            <artifactId>junit-jupiter</artifactId>
            <version>6.1.3</version>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.mockito</groupId>
            <artifactId>mockito-junit-jupiter</artifactId>
            <version>${mockito.version}</version>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-enforcer-plugin</artifactId>
                <version>3.6.3</version>
                <executions>
                    <execution>
                        <id>enforce-java-version</id>
                        <goals>
                            <goal>enforce</goal>
                        </goals>
                        <configuration>
                            <rules>
                                <requireJavaVersion>
                                    <version>[21,)</version>
                                    <message>Joular Code for Java requires Java 21 or later.</message>
                                </requireJavaVersion>
                            </rules>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>3.16.0</version>
            </plugin>
            <plugin>
                <!-- Exposes ${org.mockito:mockito-core:jar} as the resolved path to the agent jar, whatever the local repository layout is -->
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-dependency-plugin</artifactId>
                <version>3.11.0</version>
                <executions>
                    <execution>
                        <id>resolve-dependency-paths</id>
                        <goals>
                            <goal>properties</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-surefire-plugin</artifactId>
                <version>3.6.0</version>
                <configuration>
                    <!-- @{argLine} is evaluated late, so an agent that sets the argLine property (JaCoCo) composes with these flags instead of being dropped -->
                    <argLine>@{argLine} -Xshare:off -javaagent:${org.mockito:mockito-core:jar} -Dnet.bytebuddy.experimental=true</argLine>
                    <redirectTestOutputToFile>true</redirectTestOutputToFile>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-jar-plugin</artifactId>
                <version>3.5.1</version>
                <configuration>
                    <forceCreation>true</forceCreation>
                    <archive>
                        <manifestEntries>
                            <Agent-Class>org.noureddine.joular.joularcodejava.agent.Agent</Agent-Class>
                            <Premain-Class>org.noureddine.joular.joularcodejava.agent.Agent</Premain-Class>
                            <Implementation-Version>${project.version}</Implementation-Version>
                        </manifestEntries>
                    </archive>
                </configuration>
            </plugin>
        </plugins>
    </build>
</project>
